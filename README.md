# VoltWay

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Python](https://img.shields.io/badge/Python-3.8%2B-blue.svg)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.68.0%2B-green.svg)](https://fastapi.tiangolo.com/)

**VoltWay** — это веб-сервис на основе `FastAPI`, предоставляющий интерактивную карту покрытия зарядных станций для электромобилей.

Сервис агрегирует данные из открытых источников, визуализирует их на карте и предлагает удобный интерфейс для поиска и фильтрации.

## ✨ Возможности

🗺️ **Интерактивная карта:** Отображение зарядных станций на карте (`Leaflet/OpenStreetMap`) с кластеризацией меток.

🔍 **Умный поиск:** Поиск станций по локации, типу разъема (`CCS, CHAdeMO, Type 2`), мощности и текущему статусу.

⚡ **Детальная информация:** Карточка каждой станции с подробными данными: адрес, доступные разъемы, мощность, часы работы, цена (если доступно).

🌟 **Избранные станции:** Сохраняйте часто используемые станции для быстрого доступа.

🔔 **Реальные уведомления:** Получайте мгновенные уведомления о изменениях статуса станций через WebSocket.

🧭 **Построение маршрута:** Интеграция с картографическими сервисами для построения маршрута до выбранной станции.

🚀 **Быстрый API:** Backend на `FastAPI` обеспечивает высокую производительность и автоматическую генерацию документации (`Swagger/ReDoc`).

🔄 **Актуальные данные:** Периодическое обновление данных о станциях из внешних API (например, `Open Charge Map`, `API-Ninjas`).

## 🏗️ Архитектура и технологии

**Backend:**

- FastAPI (Python) — высокопроизводительный веб-фреймворк для построения API.
- SQLAlchemy + Alembic — ORM и миграции базы данных.
- PostgreSQL / SQLite — система управления базами данных.
- Redis — кэширование частых запросов.
- Pydantic — валидация данных и настройки.

**Frontend:**

- Leaflet — библиотека для интерактивных карт.
- OpenStreetMap — бесплатная картографическая основа.
- JavaScript (Vanilla или фреймворк, например, Vue.js/React для более сложного UI).

**Внешние сервисы:**

- [Open Charge Map](https://openchargemap.org/) — источник данных о зарядных станциях.
- [API-Ninjas](https://api-ninjas.com/api/evcharger) — API для информации о зарядных станциях.

## 🚀 Установка и запуск

### Предварительные требования

- Python 3.8+
- PostgreSQL или SQLite
- Redis (опционально, для кэширования)

### Быстрый старт

1. **Клонируйте репозиторий:**

   ```bash
   git clone https://github.com/QuadDarv1ne/VoltWay.git
   cd VoltWay
   ```

2. **Создайте и активируйте виртуальное окружение:**

   ```bash
   python -m venv venv
   # Для Linux/macOS:
   source venv/bin/activate
   # Для Windows:
   venv\Scripts\activate
   ```

3. **Установите зависимости:**

   ```bash
   pip install -r requirements.txt
   ```

4. **Настройте переменные окружения:**

   Создайте файл `.env` в корне проекта на основе примера `.env.example`:

   ```bash
   cp .env.example .env
   ```

   Отредактируйте `.env`, указав свои настройки (API-ключи, строки подключения к БД).

5. **Инициализируйте базу данных:**

   ```bash
   alembic upgrade head
   ```

6. **Добавьте тестовые данные (опционально):**

   ```bash
   python add_sample_data.py
   ```

7. **Запустите сервер разработки:**

   ```bash
   uvicorn app.main:app --reload
   ```

### Альтернативные способы запуска

#### Через Docker (рекомендуется):

```bash
# Запуск с полным стеком (PostgreSQL, Redis)
docker-compose up -d

# Инициализация базы данных
docker-compose exec app alembic upgrade head
docker-compose exec app python add_sample_data.py

# Просмотр логов
docker-compose logs -f app
```

#### Использование Makefile:

```bash
# Установка зависимостей
make install

# Запуск разработки
make dev-run

# Запуск тестов
make test

# Просмотр логов
make logs

# Очистка артефактов
make clean
```

### Проверка работы

После запуска откройте в браузере:

- **Приложение:** http://localhost:8000
- **Документация API (Swagger):** http://localhost:8000/docs
- **Альтернативная документация (ReDoc):** http://localhost:8000/redoc
- **Health check:** http://localhost:8000/health
- **Детальный health check:** http://localhost:8000/health/detailed

## ⚡ Последние улучшения

Проект получил значительные улучшения производительности и качества кода:

- 🏗️ **Repository Pattern** - чистая архитектура с разделением слоёв
- 📊 **Структурированные логи** - JSON формат для лучшей аналитики
- 🔍 **Request Tracing** - уникальный ID для каждого запроса
- 🚀 **Производительность** - индексы БД, сжатие ответов, connection pooling
- 🐳 **Docker оптимизация** - образы на 30% меньше, безопаснее
- 💚 **Enhanced Health Checks** - детальная проверка зависимостей
- ✅ **Больше тестов** - покрытие репозиториев

**Подробнее:** см. [IMPROVEMENTS.md](IMPROVEMENTS.md) и [QUICK_IMPROVEMENTS_GUIDE.md](QUICK_IMPROVEMENTS_GUIDE.md)

## 📁 Структура проекта

```
VoltWay/
├── app/
│   ├── api/           # Эндпоинты FastAPI
│   ├── core/          # Конфигурация, безопасность
│   ├── crud/          # Логика взаимодействия с БД
│   ├── models/        # SQLAlchemy модели
│   ├── schemas/       # Pydantic схемы
│   ├── services/      # Бизнес-логика, работа с внешними API
│   ├── static/        # Статические файлы (JS, CSS, изображения)
│   ├── templates/     # HTML шаблоны
│   └── main.py        # Точка входа приложения
├── migrations/        # Alembic миграции
├── tests/             # Тесты
├── requirements.txt   # Зависимости Python
├── .env.example       # Шаблон переменных окружения
├── API_DOCUMENTATION.md  # Полная документация API
├── USER_GUIDE.md      # Руководство пользователя
└── README.md          # Эта документация
```

## 🛠️ Использование API

### Документация

Подробная документация API доступна в отдельных файлах:

- **[API_DOCUMENTATION.md](API_DOCUMENTATION.md)** — Полная техническая документация всех эндпоинтов
- **[USER_GUIDE.md](USER_GUIDE.md)** — Руководство пользователя по работе с веб-интерфейсом

### Основные эндпоинты

- `GET /api/v1/stations` — Получить список станций с поддержкой пагинации и фильтрации.
  - Параметры: `latitude`, `longitude`, `radius_km`, `connector_type`, `min_power_kw`.

- `GET /api/v1/stations/{station_id}` — Получить детальную информацию о конкретной станции.

- `POST /api/v1/stations/update_cache` — (Админ) Принудительно обновить кэш данных из внешних источников.

- `POST /api/v1/favorites/{station_id}` — Добавить станцию в избранное

- `GET /api/v1/notifications/stats` — Получить статистику уведомлений

**Пример запроса:**

```bash
curl "http://localhost:8000/api/v1/stations?latitude=55.7558&longitude=37.6173&radius_km=10"
```

### 🤝 Развитие проекта

**Планируемые улучшения и идеи для контрибьюторов:**

- Мобильное приложение (на `React Native` или `Flutter`).
- **Расширенная аналитика:** Графики загруженности станций, история цен.
- **Интеграция с навигаторами:** Глубокие ссылки в `Яндекс.Навигатор`, `Google Maps`
- Система отзывов и рейтингов для станций.
- Telegram-бот для быстрого поиска ближайших станций.
- Расширенная система уведомлений с мобильными пуш-уведомлениями.
- Интеграция с системами бронирования зарядных слотов.

## 🤝 Contributing

Мы приветствуем вклад в проект. Пожалуйста, ознакомьтесь с [CONTRIBUTING.md](CONTRIBUTING.md) для получения инструкций по внесению изменений.

## 📄 Лицензия

Этот проект распространяется под лицензией `MIT`

**Подробнее см. в файле `LICENSE`**

## 🔗 Ссылки

- [VoltWay Website](https://volt-ev.ru/)
- [API-Ninjas EV Charger API](https://api-ninjas.com/api/evcharger)
